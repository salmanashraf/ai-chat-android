# Gemini Credentials

Place your Google Generative Language `google.json` service file in this directory with the exact name `google.json`. The sample app automatically reads the file when configuring Gemini.
